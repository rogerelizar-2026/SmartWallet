Nothing needs to be added to .gitignore since the changes only include a markdown file (.md) and a shell script (.sh), which are source/config files and not build artifacts, dependencies, or temporary files.